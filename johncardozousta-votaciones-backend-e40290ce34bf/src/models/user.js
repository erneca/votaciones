const { default: mongoose } = require("mongoose");
const { Schema, model } = require("mongoose");

//Schemas
// let usuarioVotacion = new Schema({
//   votacion: { type: Schema.ObjectId, required: true, ref: "Votaciones" },
//   usuario: { type: JSON, required: { required: true } },
// });

// // models
// let usuVotModel = model("usuario-votacions", usuarioVotacion);

// module.exports = {
//   usuVotModel,
// };
